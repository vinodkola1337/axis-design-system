{/* Copy this file into a component folder, rename it, and replace ComponentName and story references. */}

import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as Stories from './ComponentName.stories'

<Meta of={Stories} />

# ComponentName

## Overview

Write one short paragraph describing the component's purpose.

<Canvas of={Stories.Playground} />

## Usage

```vue
<script setup lang="ts">
import { ComponentName } from '@vinodkola/axis-ui'
</script>

<template>
  <ComponentName />
</template>
```

- **State the principle clearly:** Add three to five concise recommendations that help consumers make design decisions.
- **Explain the reason:** Connect guidance to user outcomes, hierarchy, consistency, or accessibility.
- **Keep implementation details elsewhere:** Reserve this section for usage decisions rather than prop definitions.

| Use | Avoid |
|---|---|
| Add two or three concrete examples of correct use. | Add the corresponding misuse to avoid. |
| Keep comparisons short and directly related. | Repeat every best-practice bullet in table form. |

## Variants

### Option Family

Add a one-line description naming the prop that controls this option family and what it changes.

<Canvas of={Stories.Options} />

| Option | Use when |
|---|---|
| Option one | Add a concise description of when to use it. |
| Option two | Add a concise description of when to use it. |

### Disabled

Name the prop that controls the disabled state and briefly explain its effect.

<Canvas of={Stories.Disabled} />

## API

<ArgTypes />

## Accessibility

### Screen Reader

Describe accessible naming, announcements, and relationships. Add a component-specific labeling example when useful.

```html
<ComponentName aria-label="Describe the action" />
```

### Keyboard Support

| Key | Function |
|---|---|
| `Enter` | Describe the interaction, or remove this row when it does not apply. |
| `Space` | Describe the interaction, or remove this row when it does not apply. |
